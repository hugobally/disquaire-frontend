import './src/styles/global.css';
import "@fontsource/oswald" // Defaults to weight 400 with all styles included.
import "@fontsource/homemade-apple" // Defaults to weight 400 with all styles included.
